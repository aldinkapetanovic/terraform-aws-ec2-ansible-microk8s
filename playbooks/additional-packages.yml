---
- hosts: all
  become: true
  tasks:
    - name: Install ZSH, Git...
      apt:
        name: "{{ packages }}"
        state: present
        update_cache: true
      vars:
        packages:
          - zsh
